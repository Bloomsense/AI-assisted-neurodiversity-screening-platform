/** Columns on the `patients` table (primary key is `patient_id`, not `id`). */
 export const PATIENT_SELECT_COLUMNS =
  'patient_id, name, age, gender, caregiver_name, caregiver_contact, email, remarks, status, profile_created_date, profile_tag, risk_level, assigned_doctor_id';