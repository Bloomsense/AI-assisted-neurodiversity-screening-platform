## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev : api`  && `npm run dev` to start the development server.

Project Deployed Link: https://ai-assisted-neurodiversity-screenin-eosin.vercel.app/