# Supabase project URL (same as used by the Deno server / Edge function)
DATABASE_URL=https://your-project-ref.supabase.co

# Service role key (server only — never expose in the browser)
DATABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional (default 8000 — matches ChildProfileDetail dev URL)
PORT=8000
